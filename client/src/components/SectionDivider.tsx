import React from "react";

const SectionDivider: React.FC = () => {
  return (
    <div className="w-full h-px bg-gradient-to-r from-transparent via-accent/30 to-transparent my-24 opacity-50" />
  );
};

export default SectionDivider;
